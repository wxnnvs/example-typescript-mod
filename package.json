{
  "name": "server-stats",
  "version": "0.1",
  "description": "get server info",
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "build-bun": "bun build --bundle ./src/main.ts --outdir=out/ --outfile=out/plugin.js --sourcemap=inline --watch --target browser --entry-naming EaglerForge-[hash].js",
    "build-es": "esbuild --bundle ./src/main.ts --outfile=out/plugin.js --sourcemap=inline --watch=forever"
  },
  "author": "wxnnvs",
  "devDependencies": {
    "css-loader": "^6.8.1",
    "ts-loader": "^9.4.4",
    "typescript": "^5.2.2",
    "webpack": "^5.88.2",
    "webpack-cli": "^5.1.4"
  }
}
